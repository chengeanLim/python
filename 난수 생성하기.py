"""
>넘파이에 난수의 시드를 설정하는 문자
->  >>> np.random.seed(100)

>시드가 설정되면 다음과 같은 문장을 수행하여 5개의 난수를 얻을 수 있음

> 난수는 컴퓨터가 규칙을 가지고 생성한 수로 정확한 의미의 난수는 아니지만 난수에
가까운 수로 의사난수라고 부른다.

>난수 생성
->  >>> np.random.rand(행, 열)
->  >>> np.random.randint(1, 7, size=10) 정수값을 뽑아오고싶을때 사
"""
