mid_scores = [10, 20, 30]
final_scores = [70, 80, 90]
mid_scores[0] + final_scores[0]

total - mid_scores + final_scores
total

import numpy as np
mid_scores = np.array([10, 20, 30])
final_scores = np.array([70, 80, 90])
mid_scores + final_scores

a = mid_scores
b = final_scores
a+b
