np_array[0,0] = 12

# linspace(start, stpo, num = 50) 0에서 10까지 총 100개의 수들이 생성

# logspace(start, stop, num = 50) logspace(x, y, n) 생성되는 수의 시작은 10x,
#부터 10y까지가 되며, n개의 수가 생성

reshape(행, 열), flatten() #flatten()은 1차원 배열로 만들어 준다.
